services:
  backend:
    image: ${IMAGE_FULL_URL:-nginx:alpine}
    container_name: zigee-backend
    restart: always
    ports:
      - "8080:8080"
    environment:
      - TZ=Asia/Seoul
      - SPRING_PROFILES_ACTIVE=dev
      - SERVER_PORT=8080