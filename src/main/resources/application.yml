spring:
  profiles:
    group:
      local: "local, datasource-local, security, kakao"
      dev: "dev, datasource-mysql, security, kakao"
      prod: "prod, datasource-mysql, security, kakao"

logging:
  level:
    academy.zigee: debug
    org.springframework: debug